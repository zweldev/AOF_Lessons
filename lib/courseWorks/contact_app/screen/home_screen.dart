import 'package:flutter/material.dart';

class ContactAppHomeScreen extends StatelessWidget {
  const ContactAppHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Contact Home"),),
    );
  }
}