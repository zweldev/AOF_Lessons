import 'package:flutter/material.dart';

class ContactSecurityQuestion extends StatelessWidget {
  const ContactSecurityQuestion({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Security Questions"),),
    );
  }
}